const firstNumber = +(prompt('Enter the first number: '));
const secondNumber = +(prompt('Enter the second number: '));

//use short if else
// firstNumber > secondNumber ? alert(`The first number ${firstNumber} is bigger`) : alert(`The second number ${secondNumber} is bigger`);

//use if else classic
if (firstNumber > secondNumber) {
    alert(`The first number ${firstNumber} is bigger`);
} else {
    alert(`The second number ${secondNumber} is bigger`);
}



