const getFirstCharacter = (str) => str[0];
const getLastCharacter = (str) => str[str.length - 1];

const handler = {
  getFirstCharacter: getFirstCharacter,
  getLastCharacter: getLastCharacter
};

export default handler;
